name: Pull Request
on:
  pull_request:
    branches:
      - master

env:
  PACKAGE_NAME: openshift4-service-mesh

jobs:
  linting:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        command:
          - lint_yaml
          - lint_adoc
          - lint_commodore
    steps:
      - uses: actions/checkout@v5
      - name: Run ${{ matrix.command }}
        run: make -C package ${{ matrix.command }}
  editorconfig:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: snow-actions/eclint@v1.0.1
        with:
          args: 'check'
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        instance:
          - defaults
    defaults:
      run:
        working-directory: ${{ env.PACKAGE_NAME }}
    steps:
      - uses: actions/checkout@v5
        with:
          path: ${{ env.PACKAGE_NAME }}
      - name: Compile component
        run: make  -C package test -e instance=${{ matrix.instance }}
  golden:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        instance:
          - defaults
    defaults:
      run:
        working-directory: ${{ env.PACKAGE_NAME }}
    steps:
      - uses: actions/checkout@v5
        with:
          path: ${{ env.PACKAGE_NAME }}
      - name: Golden diff
        run: make -C package golden-diff -e instance=${{ matrix.instance }}
