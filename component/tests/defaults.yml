# Overwrite parameters here
parameters:
  kapitan:
    dependencies:
      - type: https
        source: https://raw.githubusercontent.com/appuio/component-openshift4-operators/v1.6.1/lib/openshift4-operators.libsonnet
        output_path: vendor/lib/openshift4-operators.libsonnet

  openshift4_operators:
    defaultSourceNamespace: openshift-marketplace
