parameters:
  openshift4_service_mesh:
    =_metadata:
      multi_tenant: true

    namespace: syn-openshift-service-mesh
    operator:
      channel: stable
      installPlanApproval: Automatic

    es_operator:
      enabled: true
      channel: stable
      installPlanApproval: Automatic
